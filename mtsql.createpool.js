
// var mysql      = require('mysql');
// var connection = mysql.createConnection({
//   host     : 'localhost',
//   user     : 'root',
//   password : 'root',
//   database : 'newdb'
// });

// connection.connect()

// connection.query('select b.id, b.name, u.id, u.name from Booktbl b, Usertbl u, UserBook u where b.id= ub.Bookid AND u.id = ub.userId')
 
